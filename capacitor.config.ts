import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Solamun_app_v0.1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
